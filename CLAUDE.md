# HANDOVER POINT

Last updated: 11 June 2026
Status: GitHub Actions migration COMPLETE; token + schedule + session-expiry fixes applied

---

## What just happened

Migrated from Windows Task Scheduler to GitHub Actions with a self-hosted runner. This is now the live production setup.

June 2026 fixes:
- Workflow now has a real cron schedule (it previously only ran on manual dispatch, despite this doc claiming hourly).
- The Refresh button no longer embeds a GitHub PAT in index.html — GitHub auto-revokes any PAT found in a public repo, which is why the button kept dying with 401. It now prompts once per browser and keeps the token in localStorage.
- generate_dashboard.py now FAILS the workflow run when the SAASIT session has expired, instead of silently publishing an all-zero dashboard. A red run in the Actions tab = session needs refreshing.
- login.py / generate_dashboard.py read the PAT from the HRIS_GITHUB_PAT env var or a git-ignored github_pat.txt file — never hardcode it in source.

## Current Architecture

| Component | Detail |
|-----------|--------|
| Trigger | GitHub Actions cron (hourly Mon-Fri, 7-16 UTC = 8am-5pm BST) + workflow_dispatch |
| Runner | Self-hosted on DESKTOP-MJDJM64 |
| Runner location | C:\\actions-runner\\ |
| Runner service | Windows service — auto-starts on boot |
| Schedule | .github/workflows/update-dashboard.yml |
| Python setup | Removed (pre-installed on runner) |
| Commit step | PowerShell |
| Home access | No VPN — Oxford MDM-enrolled |

## Day-to-Day

Nothing to do. Just Refresh your browser.
Refresh Session.bat only needed when session expires (~every few weeks — workflow runs go red and the dashboard stops updating; it no longer shows zeros).

## Git Hygiene

Always git pull --no-edit before git push (runner auto-commits index.html).

## Session Expiry

1. Put a valid GitHub PAT (repo + workflow scope) in github_pat.txt next to login.py, or set HRIS_GITHUB_PAT (one-time setup; the file is git-ignored).
2. Run Refresh Session.bat from C:\\Users\\admin\\Documents\\Claude\\HRIS-Dashboard\\ and log in via Oxford SSO.
3. login.py pushes the session to the SAASIT_SESSION GitHub secret; the next workflow run picks it up.

---
Generated by Claude (Cowork / AIMM chain) — Lelitte Co.

# HANDOVER.md — hris-dashboard

Last updated: 27 August 2026
Status: Live and working. 27 Aug: (1) manual `.bat` "not working" report investigated — fresh export was already live (7b50878), no bug reproduced. (2) Kevin approved Option 1 + Option 3: `import_osm_report.py` hardened (lazy xlrd, locked-file retry, explicit-file override, advisory lock, push race-retry; schema unchanged), Desktop `.bat` re-synced, and a Startup-folder Downloads watcher built + deployed + LIVE (pinned to bd93285). Morning Scheduled Task untouched. GitHub Actions SAASIT scrape still needs Kevin's SSO re-login (pre-existing).

---

## Current State

The dashboard is fully operational at https://begb0037admin.github.io/hris-dashboard/

- Ticket data loaded from `data/tickets.json` (generated by `import_osm_report.py` + `generate_dashboard.py` via GitHub Actions or bat script)
- Linda AI auto-analyses the queue on page load, then accepts free-text questions
- Simon Burford excluded from display and Linda analysis (`EXCLUDED_ANALYSTS` filter in JS)
- Ticket numbers display correctly from `task_num` field in tickets.json

---

## Architecture

| Component | Detail |
|---|---|
| `index.html` | Single-file static dashboard. All CSS, HTML, JS inline. No framework. |
| `data/tickets.json` | Generated by `generate_dashboard.py`. Fields: `task_num`, `summary`, `status`, `priority`, `days`, `analyst`, `category`, `created`, `team`. |
| `generate_dashboard.py` | Parses OSM report → writes `data/tickets.json`. Fails workflow run on SAASIT session expiry. |
| `import_osm_report.py` | Imports raw OSM Excel/CSV export → intermediate format. |
| `login.py` | Oxford SSO login via Playwright. Saves `session.json`. |
| `Refresh Session.bat` | Pulls latest `login.py` from GitHub and runs it to refresh session. |
| `Update HRIS Dashboard.bat` | Runs the full pipeline locally if needed. |
| `.github/workflows/` | GitHub Actions — hourly cron Mon-Fri 7-16 UTC on self-hosted runner (DESKTOP-MJDJM64). |
| `cloudflare-worker/` | Source for Cloudflare Worker proxy (Refresh button — PAT stored as Worker secret). |

---

## Linda AI

- Worker: `https://hr-kb-ai.kevinlelitte.workers.dev/` (shared with HR FA Knowledge Base)
- CORS: `ALLOWED_ORIGIN` env var in Cloudflare = `https://kb.lelitte.co.uk,https://begb0037admin.github.io`
- When dashboard moves to `oms.lelitte.co.uk`: update `ALLOWED_ORIGIN` to `https://kb.lelitte.co.uk,https://oms.lelitte.co.uk` — remove GitHub Pages origin
- Linda analyses each analyst in sequence; overall summary first, then per-analyst with dividers
- Input unlocks after all auto-analysis is complete
- Paste full ticket thread into Linda for deep-dive; she can cross-reference the FA Knowledge Base

---

## Colour System (established 03 Jul 2026)

Unified palette used by both Linda insight cards and status pills:

| Meaning | Linda card class | Status pill class | Colours |
|---|---|---|---|
| Summary / neutral | `linda-insight-main` | `s-assigned` | Oxford blue: `#e8f0f7` bg, `#1a3a5c` text, `#b8d0e8` border |
| Warning / needs action | `linda-insight-warn` | `s-waiting` / `s-logged` | Red: `#fff1f0` bg, `#9f1239` text, `#fecaca` border |
| Healthy / ok | `linda-insight-ok` | `s-accepted` | Green: `#f0fdf4` bg, `#166534` text, `#bbf7d0` border |
| Informational | `linda-insight-info` | `s-default` | Grey: `#f8f9fa` bg, `#6b7280` text, `#e5e7eb` border |

---

## Analyst Roles (hardcoded in JS)

```js
var ANALYST_ROLES = {
  'Kevin Lelitte': 'FA Team Lead',
  'James Salas Guillen': 'Health & Safety Systems FA Senior Analyst',
  'Asta Palmer': 'FA Analyst',
  "Michael O'Sullivan": 'FA Analyst'
};
var EXCLUDED_ANALYSTS = ['Simon Burford'];
```

To add or rename an analyst: edit `ANALYST_ROLES` and `EXCLUDED_ANALYSTS` in `index.html` and push.

---

## Restore Points

| File | Date | Notes |
|---|---|---|
| `Archive/index_backup_20260703_1005.html` | 03 Jul 2026 | Before colour unification. Last pre-session state. |

Live index.html SHA after session: `6118495b56e265d0ca07b588bc8ba7e5f5cef8ad`

---

## Session Log — 03 July 2026

Changes made this session (all pushed to main, approved by Kevin):

1. **Inter font** applied throughout
2. **Analyst role titles** added below names in card headers and Linda dividers
3. **Linda panel widened** from 360px to 450px
4. **Header subtitle** colour changed to Oxford blue (`#3d6b8f`)
5. **Ticket column** header updated to "Ticket No."; field fixed to `task_num`
6. **Simon Burford excluded** from dashboard display and Linda analysis
7. **CORS fix** — Cloudflare Worker `hr-kb-ai` updated to support comma-separated `ALLOWED_ORIGIN`; Kevin updated Cloudflare var to `https://kb.lelitte.co.uk,https://begb0037admin.github.io`; Linda now connects from GitHub Pages
8. **Linda insight card colour system** — ✦ pale Oxford blue (summary), ⚠ amber-red (warnings), ✓ green (ok), ℹ pale grey (info); icons enlarged to `1.1rem`; text `0.82rem`; bold on summary/warn
9. **Status pills unified** to same colour palette as Linda cards; border added; `font-size: 0.7rem`
10. **Table typography** — `font-size: 0.82rem`, `th font-weight: 700`, `td font-weight: 500`, `padding: 10px 14px`

---

## Known Issues / Watch Items

- Linda worker is shared with HR FA Knowledge Base — if KB worker is redeployed, check `ALLOWED_ORIGIN` still includes GitHub Pages
- Dashboard will move to `oms.lelitte.co.uk` — when that happens, update `ALLOWED_ORIGIN` and remove the GitHub Pages entry
- `generate_dashboard.py` fails the workflow (goes red) when SAASIT session expires — run `Refresh Session.bat` to fix

---

## Next Steps

- [ ] Move dashboard to `oms.lelitte.co.uk` (Kevin to set up domain + Cloudflare Pages)
- [ ] Update `ALLOWED_ORIGIN` when domain is live
- [ ] Consider adding ticket detail expand / click-through to OSM when on domain
- [ ] HANDOVER.md does not yet exist — created this session


---

## Session 2026-08-11 -- Missing schedule trigger fixed; SAASIT session still needs Kevin's login (Drew, not this repo's usual agent)

**Scope:** Kevin asked Drew to investigate why this dashboard wasn't updating (separate from Drew's usual three repos), then approved fixing what was found.

**Root cause confirmed by reading the live workflow file directly, not the docs:** `.github/workflows/update-dashboard.yml`'s `on:` block had regressed to `workflow_dispatch:` only -- no `schedule:` trigger at all, despite `CLAUDE.md`/`README.md`/`HANDOVER.md` all documenting "hourly Mon-Fri 7-16 UTC." Checked the workflow's real run history via the GitHub API: last attempt of any kind (success or failure) was 2 July 2026, over 5 weeks stale.

**Fixed:** re-added `schedule: - cron: '0 7-16 * * 1-5'` alongside the existing `workflow_dispatch:` trigger, matching what the docs always claimed.

**Verified with a real run, not just that the YAML looks right:** manually dispatched the workflow after pushing the fix. Result: **run #57 completed (not stuck queued, not failing to trigger) but concluded `failure`** -- confirms the schedule/trigger bug itself is genuinely fixed (the workflow now runs end-to-end on the self-hosted runner and reaches the real failure point), but the underlying SAASIT session is still expired. `last_run_status.txt` shows the exact same `OData auth error 401 (ISM_4001)` as the 2 July failure, against the same stale `session.json` (still last modified 11 June 2026 -- unchanged).

**Not fixed, and not something Drew can fix:** `login.py`'s own docstring is explicit -- refreshing the SAASIT session requires opening a browser and logging in via **Oxford SSO with Microsoft MFA**, interactively, as Kevin. This is outside what any agent should do on Kevin's behalf (credential/MFA entry). **Kevin needs to run `Refresh Session.bat` (or `python login.py`) himself** -- once that's done, the now-restored hourly schedule will pick the fresh session up automatically on its next run; no further engineering step needed after that.

---

## Session 2026-08-20 -- `.xls` support added to the manual OSM import pipeline (Drew; hris-dashboard now formally in Drew's scope)

**Scope:** Kevin formally added `hris-dashboard` to Drew's accountable scope this session. Task: OSM Administration's "All Open Tasks by Team" report sometimes arrives as a genuine old-format `.xls` (OLE binary) rather than `.xlsx`; `import_osm_report.py` only ever matched/read `.xlsx`, so the drop-into-Downloads-and-run-the-`.bat` workflow would fail outright on an `.xls` attachment. Today's live example: `All Open Tasks by Team.xls` from `reports-prd-ldz@saasiteu.com` (cc Louise Piper), 20 Aug 2026.

**Fixed:** `import_osm_report.py` now handles both formats natively, no manual conversion step:
- `FILE_PATTERN` widened to `FILE_PATTERNS` (globs both `*.xlsx` and `*.xls`, most-recently-modified wins across both, same selection semantics as before).
- Added an `xlrd`-based reader (`_read_rows_xls`) for `.xls`, normalised to the same row-tuple shape `openpyxl` already produced, so all downstream header-detection/column-mapping/grouping logic is untouched and format-agnostic.
- `Update HRIS Dashboard.bat` now also `pip install`s `xlrd`.
- Commits on `main`: `import_osm_report.py` → `f418802dac35445cbeaa0585d1276d3d963af015`; `Update HRIS Dashboard.bat` → `a23f1dd0cef25bb9739397636687bc2096fea0e9`.

**Verified for real, not just read-through:** built a synthetic OLE-binary `.xls` (confirmed via `file` as `CDFV2 Microsoft Excel`, matching the brief's claim) reproducing the report's shape, and ran the new `parse_report()`/`find_report()` directly against it — 3 tickets parsed correctly including date conversion and the unassigned-analyst bucket. Also verified the pre-existing `.xlsx` path is unchanged, and that `find_report()` correctly picks whichever format is genuinely most recently modified when both are present. Full detail in `RESUME.md` (new this session).

**NOT verified:** the actual `.bat` was not run end-to-end on Kevin's machine, and Kevin's real 20 Aug `.xls` attachment (as opposed to a synthetic stand-in) was never opened. Low risk (same proven header-detection-by-name logic as the existing `.xlsx` path) but not zero. **Kevin's next action:** drop today's `All Open Tasks by Team.xls` into `C:\Users\admin\Downloads`, run `Update HRIS Dashboard.bat`, confirm it completes and the live dashboard updates.

**Flagged, not fixed (both explicitly out of scope for this pass):**
1. Supply-chain gap: `Update HRIS Dashboard.bat` pulls `import_osm_report.py` from `raw.githubusercontent.com` on every run with no commit pin or hash check.
2. Sender domain `saasiteu.com` on today's report: checked against this repo's own live `last_run_status.txt` evidence — `oxford.saasiteu.com` is the same domain the automated Playwright scrape has authenticated against all along, so this reads as SAASIT's genuine vendor domain, not a new/suspicious one. Not a definitive email-header check; Kevin's own call on the actual email stands.

**Unrelated, still open:** the automated GitHub Actions scrape path is still failing on the same expired-SAASIT-session issue found 11 Aug 2026 (confirmed still failing as of run #112, 20 Aug 08:37 UTC) — needs Kevin's interactive Oxford SSO+MFA login, unrelated to the fix above.

`RESUME.md` created this session (previously absent) — see it for the full durable-state record, verification detail, and exact next action.

---

## Session 2026-08-20 (same-day follow-on) -- Supply-chain gap fixed: `.bat` download now pinned to a commit SHA (Drew, Kevin-approved)

Kevin approved fixing the supply-chain gap flagged above. `Update HRIS Dashboard.bat` no longer downloads `import_osm_report.py` from the floating `main` branch URL — it now uses a commit-pinned raw URL (`raw.githubusercontent.com/begb0037admin/hris-dashboard/<SCRIPT_SHA>/import_osm_report.py`), with `SCRIPT_SHA` set near the top of the `.bat` and an inline comment explaining the pin and how to bump it. Verified `main`'s tip live before pinning (`gh api repos/begb0037admin/hris-dashboard/commits/main`, not trusted from memory) — `e503509d2785cc30d4102365aa72e353983f864d`, includes the `.xls` fix above. Sanity-checked the SHA-pinned raw URL actually resolves (HTTP 200, correct content) before pushing. Commit: `7b3014beb9d696c4d2d40da2d1069bc87996ed4b`.

**Behaviour change worth flagging loudly:** before today, the `.bat` always ran whatever `import_osm_report.py` looked like on `main` — `main` was live. That's no longer true. **A future edit to `import_osm_report.py` has zero effect on the `.bat` until `SCRIPT_SHA` is deliberately bumped to the new commit and pushed** — otherwise Kevin silently keeps getting today's pinned version forever, with no error. Any session touching `import_osm_report.py` going forward must also bump `SCRIPT_SHA` in the same change (or explicitly note the pin is being left as-is on purpose). Full detail in `RESUME.md`.

---

## Session 2026-08-20 (same-day, evening follow-on) -- OSM morning auto-refresh built and live-tested; Task Scheduler job staged but NOT enabled (Drew)

**Scope:** New request from Kevin, same day hris-dashboard formally joined Drew's scope. A second OSM report now arrives by email automatically every morning (`reports-prd-ldz@saasiteu.com`, subject "Report: All Open Tasks by Team", lands in Outlook folder `Inbox/Reports/OSM`, ~08:01-08:16 daily without fail) -- separate from the pre-existing manual Excel-download workflow. Kevin wants it picked up automatically ~8:30am, saved where the existing pipeline already looks, and the existing update pipeline triggered, with basic failure detection so a silent break (sender/subject/format change) doesn't leave the dashboard quietly stale. Explicit instruction: build and test everything, but do NOT enable the actual Task Scheduler job without his go-ahead, and do NOT touch the existing manual update path/script logic.

**Built, both pushed to `main`:**
- `fetch_osm_report.py` (new) -- Outlook COM pull. Finds today's OSM email in `Inbox/Reports/OSM`, saves its attachment to Downloads as `All Open Tasks by Team - auto.xls`/`.xlsx` (matches `import_osm_report.py`'s existing glob pattern with zero changes needed there). Fails loudly and specifically (not silently) if Outlook is unreachable, the folder is missing, no matching email arrived today, or the attachment doesn't look right -- this is the silent-break detection Kevin asked for. Supports `--dry-run DIR` for safe verification.
- `Run_HRIS_Auto_Refresh.bat` (new) -- orchestrator. Downloads+runs `fetch_osm_report.py` fresh from GitHub `main`; only on success, downloads+runs the existing, completely unmodified `Update HRIS Dashboard.bat` (via `call "...bat" < NUL` to satisfy its own `pause` unattended, without editing that file). Logs every run, timestamped, to `Documents\Claude\Projects\HRIS-Dashboard\osm_auto_refresh_last_run.log`.
- Desktop launcher files (local-only, not committed -- same category as the existing Work Inbox Briefing Desktop artifacts), staged but INERT: `Run HRIS Auto-Refresh.bat`, `Run HRIS Auto-Refresh Hidden.vbs` (mirrors `Run Inbox Briefing Hidden.vbs` exactly -- hidden window, propagates real exit code, fires a toast via the **existing** `Show-TaskNotification.ps1`, reused not reinvented), and `Register-HRISAutoRefreshTask.ps1` (Task Scheduler registration -- prepared, matches the live "Work Inbox Briefing" task's exact config, **NOT RUN**).

**Verified live and for real, not just read-through:** live-probed the actual Outlook mailbox to locate the real folder (`Inbox/Reports/OSM`, not literally top-level "OSM" as Kevin's shorthand implied); confirmed sender/subject/attachment shape against the 10 most recent real emails; ran `fetch_osm_report.py --dry-run` against the real live mailbox and successfully saved and parsed (via `xlrd`) today's real attachment, confirming its header row matches what `import_osm_report.py` already expects; ran the actual just-pushed `Run_HRIS_Auto_Refresh.bat` logic end-to-end (test copy, scratch paths) and confirmed it really downloads from `raw.githubusercontent.com/.../main` and runs successfully; found and fixed a real bug (literal space in a raw GitHub URL fails via curl -- needed `%%20` escaping) before it could bite in production; confirmed `call "x.bat" < NUL` reliably dismisses `pause` and propagates the real exit code, which Step 2 depends on. Also discovered (informational only, not fixed, out of scope) that the Desktop's existing `Update HRIS Dashboard.bat` copy is stale (2 Jul 2026, missing the `.xls`/SHA-pin fixes) -- irrelevant to the new automation since it always pulls a fresh copy from `main`, but flagged for Kevin's awareness.

**NOT run, deliberately:** the actual `Update HRIS Dashboard.bat` pipeline (would push real `data/tickets.json` to GitHub -- a real production write, out of bounds without approval) and the Task Scheduler registration itself. **Kevin's only remaining action:** review `Register-HRISAutoRefreshTask.ps1` on the Desktop and either run it or tell Drew to. Full detail, including the exact trigger schedule decision (confirmed live: the OSM report is a genuine 7-day feed, not weekdays-only -- real Saturday/Sunday emails found in the folder) in `RESUME.md`.

---

## Session 2026-08-21 -- Task Scheduler job registered and LIVE; found and fixed a real bug in the staged script before it would have silently no-op'd (Drew, Kevin's explicit go-ahead)

**Scope:** Kevin gave explicit go-ahead to enable the morning auto-refresh automation staged the prior evening. Task: run `Register-HRISAutoRefreshTask.ps1`, verify the resulting Task Scheduler job is genuinely correct (not just that the script exits 0), and checkpoint.

**Bug found and fixed before it could cause a silent failure:** the staged script's `New-ScheduledTaskSettingsSet -DisallowStartIfOnBatteries -StartWhenAvailable ...` call errored on this machine's ScheduledTasks module version -- `-DisallowStartIfOnBatteries` is not a recognised parameter here (`ParameterBindingException: A parameter cannot be found`). Because that error is non-terminating and the script's own `Write-Host "Task '...' registered..."` line at the end runs unconditionally regardless of what happened above it, the **first run appeared to succeed but had not actually registered anything** -- confirmed by immediately checking `Get-ScheduledTask` afterward, which came back empty. This is exactly the "verify the live thing, not the doc/script output about it" failure mode -- the script's own success message could not be trusted at face value.

**Fix:** confirmed live (`New-ScheduledTaskSettingsSet` with no flags, inspected the result) that `DisallowStartIfOnBatteries` defaults to `True` on this module version with no explicit flag needed, and that this matches the live "Work Inbox Briefing" task's own real `Settings` (`DisallowStartIfOnBatteries: True`) exactly -- so simply dropping the unsupported flag from `Register-HRISAutoRefreshTask.ps1` reproduces the intended config with zero behaviour change. Edited the staged script in place (Desktop copy only) with an inline comment explaining why. Re-ran it; this time `Register-ScheduledTask` returned a real task object (`State: Ready`) rather than erroring silently past to the same misleading final message.

**Verified directly against the live Task Scheduler state, not the script's own claim:**
- `Get-ScheduledTask -TaskName 'HRIS Dashboard Morning Refresh'` → exists, `State: Ready`, `Settings.Enabled: True`.
- Trigger: `MSFT_TaskDailyTrigger`, `StartBoundary 2026-08-21T08:30:00+01:00`, `DaysInterval 1` -- daily at 08:30, every day of the week as intended (not weekdays-only).
- Action: `wscript.exe "D:\OneDrive - lelitte.com\Desktop\Run HRIS Auto-Refresh Hidden.vbs"`, `WorkingDirectory` = the Desktop folder.
- Principal: `UserId admin`, `LogonType Interactive`, `RunLevel Limited` -- matches the live "Work Inbox Briefing" task exactly, confirmed by comparing both directly rather than assuming.
- Settings: `DisallowStartIfOnBatteries: True`, `StartWhenAvailable: True`, `ExecutionTimeLimit: PT20M`.
- Task Scheduler service (`Schedule`) confirmed `Status: Running`.
- All three files the Action chain depends on confirmed present on disk via `Test-Path`: `Run HRIS Auto-Refresh Hidden.vbs`, `Run HRIS Auto-Refresh.bat`, `Show-TaskNotification.ps1`.

**Deliberately NOT run this session:** the actual pipeline (would trigger a real, unscheduled `data/tickets.json` push to GitHub outside the intended 08:30 window -- out of scope for "register and verify the job," not something Kevin asked for). The automation's first real execution will be tomorrow's (22 Aug 2026) scheduled 08:30 run.

**What Kevin should expect tomorrow (22 Aug) at 08:30:** the hidden `.vbs` runs the two-step pipeline silently (no visible window), then fires a desktop toast notification either way via the existing `Show-TaskNotification.ps1` (BurntToast) -- "Success" if both the OSM email fetch and the dashboard update completed, "Failure" if either step failed (e.g. no email arrived yet, Outlook COM issue, or the dashboard pipeline itself errored) with the log path included. The full run log lands at `C:\Users\admin\Documents\Claude\Projects\HRIS-Dashboard\osm_auto_refresh_last_run.log` regardless of outcome. Task Scheduler's own "Last Run Result" column for `HRIS Dashboard Morning Refresh` will also reflect the real exit code. The existing manual midday path (`Update HRIS Dashboard.bat`) is completely unaffected and still works exactly as before.

**This turned out to be wrong -- the 21 Aug 08:30 run died silently anyway. See the next session entry below for the real root cause(s) and fix.**

---

## Session 2026-08-21 (afternoon) -- Silent Step-2 failure root-caused and fixed (two independent bugs); automated-run status added to the dashboard (Drew, Kevin's explicit go-ahead to proceed on all three without checking back first)

**Scope:** The 08:30 scheduled run fired on schedule; Step 1 (`fetch_osm_report.py`) succeeded (log shows the real attachment found and saved); Step 2 never started -- no error in the log, nothing in Windows event logs. Kevin gave three explicit instructions: (1) root-cause and fix the silent failure, with finer-grained diagnostics so a future failure pinpoints the exact line/step; (2) add a real "did this morning's automated run happen and did it work" indicator to the dashboard itself, distinct from the existing manual-update timestamp; (3) manually run the real pipeline now against today's already-fetched attachment to catch the dashboard up. Pre-authorized to proceed on all three without checking back.

**Root cause 1 -- confirmed via live bisection, not guesswork:** `Task Scheduler`'s own operational log showed the task completing in 10 seconds with return code `2147942655` (`0x800700FF`), and reproducing the run visibly (not hidden) showed cmd.exe's own parser error directly: `- was unexpected at this time.` Bisecting the real script line-by-line (safe scratch copies, real preamble + progressively more of the real body) isolated it to the failure-branch echo inside `if not "%FETCH_EXIT%"=="0" ( ... )`:
```
echo [%RUN_TS%] Step 1/2 FAILED (exit %FETCH_EXIT%) - Step 2 SKIPPED. ...
```
cmd.exe's block parser ends a parenthesized `if`/`for` block at the **first** unescaped `)` it meets while scanning -- it does not balance against an earlier `(` in the same text -- so the `)` immediately after `%FETCH_EXIT%` was read as the block's real close. Everything the script intended to run after that point (the rest of that line, the remaining body lines, and the block's real closing `)`) was misparsed as stray top-level tokens, starting with the literal text ` - Step 2 SKIPPED...`, whose leading `-` produces cmd's classic `- was unexpected at this time.` -- written only to the console, which is always invisible since this runs fully hidden via `wscript.exe` windowStyle 0. That is exactly why nothing appeared in the log or in Windows event logs: this happens at cmd's own parse layer, before any of the script's own log-append statements are ever reached. **This fired on every run regardless of Step 1's real outcome** -- cmd.exe must fully parse an if-block to know where to resume even when the condition is false and the block is being skipped, which is exactly what happened this morning (Step 1 genuinely succeeded; the block should never have been entered at all, yet the script still died parsing it). Confirmed the fix (reword the message to avoid literal parens entirely, rather than relying on remembering to `^(`/`^)`-escape) resolves it via the same bisection method before touching the real files.

**Root cause 2 -- a second, independent bug found only once root cause 1 was fixed:** re-running the (now bug-1-fixed) script for real got past Step 1 and into Step 2, then failed cleanly with `'"Update HRIS Dashboard.bat"' is not recognized as an internal or external command`. Isolated this directly (safe dummy `.bat` files, not the real `Update HRIS Dashboard.bat`, to avoid triggering real `pip install`/GitHub pushes/hanging `pause` prompts during testing): **this machine's `cmd.exe` cannot launch a second `.bat`/`.cmd` FILE from a `cmd.exe` process that is itself already executing a batch file** -- confirmed via `call "x.bat"`, bare `x.bat`, and a freshly spawned `cmd /c "x.bat"` all failing identically, with `Test-Path`/`dir`/`where` all confirming the target file genuinely exists in the correct working directory. Confirmed this is specific to **script-file** targets, not a blanket "cmd can't spawn cmd": nested `cmd /c dir`, `cmd /c echo`, and even double-nested `cmd /c "cmd /c echo"` all worked fine from the exact same nested context, and curl.exe/python.exe (real `.exe` targets) already worked fine live in Step 1. Confirmed the fix empirically before applying it live: routing the invocation through PowerShell as an intermediary succeeds every time from the same nested context --
```
powershell -NoProfile -Command "Get-Content 'empty_stdin.txt' | & '.\Update HRIS Dashboard.bat'; exit $LASTEXITCODE"
```
-- tested against a real dummy containing a genuine `pause` first, confirming it doesn't hang (the piped empty file gives `pause` an immediate EOF, same job `< NUL` used to do for the old, now-broken-here `call` invocation) and that the real exit code propagates correctly, before applying it to the real file.

**Finer-grained diagnostics, per Kevin's explicit ask:** the whole run now executes as `call :main > "%LOG_FILE%" 2>&1` -- one redirection covering the entire subroutine, both stdout *and* stderr -- instead of the old scattered per-line `>> %LOG_FILE%` appends. This means **any future cmd.exe parser error, not just this specific one, lands in the log file automatically**, because the redirection applies to everything that happens during that call, including cmd's own error output. Before this change, a script-level syntax error was structurally invisible no matter how much per-line logging existed, since execution never reached any of those log-append statements once cmd's parser choked -- which is exactly what happened this morning. Also: all three `curl` downloads (`fetch_osm_report.py`, `Update HRIS Dashboard.bat`, `push_automation_status.py`) now use `-f` (`--fail`), so an HTTP error (404/5xx) returns a real nonzero exit code instead of silently saving the error page's body as if it were the file -- this actually bit during testing (`push_automation_status.py` 404'd because it hadn't been pushed to `main` yet; without `-f`, curl saved the literal 14-byte string `404: Not Found` as the "script", which python then failed on with a confusing `SyntaxError` instead of a clear download-failed message).

**New -- dashboard automated-run status indicator (Kevin's ask #2):** `push_automation_status.py` (new file, repo root) pushes `data/last_automated_run.json` to GitHub via the same `GITHUB_PAT` + Contents-API pattern already used by `import_osm_report.py`, called by `Run_HRIS_Auto_Refresh.bat` at **every** exit point -- success and every distinct failure mode (fetch-script-download-failed, fetch-failed, update-bat-download-failed, dashboard-update-failed) -- so it's the automation's own self-reported record of what happened on its last attempt, independent of whether `tickets.json` itself changed. `index.html` now shows a badge -- **"Last automated refresh: `<time>` — Success/Failed"** -- next to the existing manual-update subtitle line, styled with the same green/red palette already established for status pills and Linda insight cards (`HANDOVER.md`'s own "Colour System" section, unchanged). Sourced from this new JSON file via a `loadAutomationStatus()` function mirroring the existing `loadData()` fetch pattern exactly; falls back to a neutral grey "No automated run recorded yet" state if the file doesn't exist (e.g. before this feature's first run) rather than erroring. This directly addresses Kevin's core complaint: a failed automated run leaving yesterday's `tickets.json` in place would otherwise look identical to a fresh successful run, since the old subtitle only ever reflected whoever last successfully finished the *manual* pipeline's own timestamp.

**Verified live, for real, this session:**
- Both bugs reproduced via isolated bisection against safe scratch/dummy files before either fix was applied to the real script.
- Ran the actual fixed orchestrator end-to-end for real, twice. First run (bug 1 fixed, bug 2 not yet found) failed cleanly at Step 2 with the new clear "not recognized" error instead of a silent parser crash -- confirming bug 1 was genuinely fixed and surfacing bug 2. Second run (both fixes applied) completed successfully: fetched today's real OSM email attachment from the live mailbox, ran the real **unmodified** `Update HRIS Dashboard.bat` → `import_osm_report.py` pipeline, parsed 31 real tickets, and pushed `data/tickets.json` (SHA `0e0c53b94c61a52fdce8aeef2034f59dcf8faf8d`) -- this same run is also today's real data catch-up (Kevin's ask #3), since the scheduled 08:30 run never got this far. `push_automation_status.py` pushed `data/last_automated_run.json` (`status: success`, `step: dashboard_update_ok`) in the same run.
- Confirmed both JSON endpoints live via GitHub Pages directly (`curl https://begb0037admin.github.io/hris-dashboard/data/tickets.json` and `.../data/last_automated_run.json`) and that the rebuilt `index.html` on Pages contains the new `main-header-auto` CSS/markup. **Not verified:** an actual browser screenshot of the rendered badge -- no browser/screenshot tool was available this session. The JS depends only on the two JSON endpoints above (both confirmed live and correctly shaped) and mirrors the already-proven `loadData()` pattern, but the literal pixel rendering hasn't been visually confirmed.
- Confirmed `Update HRIS Dashboard.bat` and `import_osm_report.py` (the manual pipeline) are untouched -- only the automated orchestrator's *invocation method* for Step 2 changed, not the target file or what it does.

**Deployed:** `D:\OneDrive - lelitte.com\Desktop\Run HRIS Auto-Refresh.bat` (CRLF line endings, matching this machine's convention -- confirmed via byte-level check) -- this is the file Task Scheduler's `HRIS Dashboard Morning Refresh` task actually runs (via the unchanged `.vbs` wrapper). Also pushed to `main` (`Run_HRIS_Auto_Refresh.bat`, `push_automation_status.py`, `index.html`) so a future re-copy to Desktop carries the fix forward. Commit `b0ad42c` (a GitHub Actions diagnostics-bot commit landed on `main` mid-session; merged cleanly as `f4fe3d6`).

**Next scheduled run:** tomorrow, 22 Aug 2026 08:30 -- the first real end-to-end test of both fixes under the actual hidden Task-Scheduler execution context (today's live verification ran visibly via PowerShell as the outer launcher, not via `wscript.exe`, though bug 2's PowerShell-intermediary fix was specifically chosen and tested because the `< NUL` approach it replaces was already proven live under the real hidden context back in the 20 Aug session). **If tomorrow's run still shows a failure toast:** `osm_auto_refresh_last_run.log` will now contain the real reason (including any future parse error, per the finer-grained-diagnostics change above) rather than stopping short -- check that file first, it should no longer be possible for it to go silent.

Full technical detail, including the exact bisection trail for both bugs, in `RESUME.md`'s superseding "latest" entry (same session).

---

## Session 2026-08-22 (late morning) -- 08:30 "no email today" discrepancy investigated live; confirmed a genuine mail-rule filing delay, not a script bug or folder mismatch (Drew, Kevin's explicit ask to investigate this morning's report-only finding)

**Scope:** Kevin reported seeing today's OSM email arrive in `Inbox/Reports/OSM` at 8:06am, contradicting this morning's 08:30 automated run, which logged "no email from `reports-prd-ldz@saasiteu.com` received today" (see RESUME.md's 08:30 entry, logged report-only per that session's own instructions, no fix attempted then). Kevin separately re-emphasized mid-task to explicitly verify the exact folder path (`Inbox > Reports > OSM`) the script queries matches what he sees in Outlook's UI, in case a folder-path mismatch was the real bug.

**Folder path -- confirmed correct, no mismatch.** Live Outlook COM probe (read-only) opened the exact same path `fetch_osm_report.py` hard-codes (`OSM_FOLDER_PATH = ["Reports", "OSM"]` under the default store's Inbox) and confirmed it is the one real folder in the tree named this way -- full folder-tree dump under Inbox showed no other similarly-named folder anywhere else in any of the 5 connected stores. `EntryID`/`StoreID` resolved cleanly. This folder is genuinely what Kevin sees in the Outlook UI as `Inbox > Reports > OSM`.

**Root cause -- confirmed live, not inferred:** this morning's 08:30 log (captured in RESUME.md before this session started) recorded the OSM folder as having **52 items** at 08:30:04, with the most recent matching email still yesterday's (21 Aug 08:01:22). A fresh live probe at 11:58 today found the folder now holding **53 items**, with today's email present (`ReceivedTime` 2026-08-22 08:06:47, subject/sender matching exactly). This folder receives exactly one message of this kind per day via a mail rule and nothing else, so the 52→53 delta is direct, not inferred, evidence that today's email was filed into this specific watched folder **after** the 08:30 script ran -- even though the message's own `ReceivedTime` property (stamped at actual mail delivery, not at rule-filing time) reads as 8:06, before 08:30. This is explanation (a) from the brief: a mail-rule/filing delay between when the email was received by the mail system and when it actually landed in the `Inbox/Reports/OSM` folder the script polls -- **not** a script bug in date/timezone comparison, and not a folder-path mismatch.

**No code bug found or fixed.** `fetch_osm_report.py`'s date-comparison logic (`received_date == today`) is working exactly as designed: it correctly read the real, live folder state at the moment it ran (52 items, no today-dated match yet) and failed loudly with a specific, accurate diagnosis -- exactly the "silent break made visible" behaviour this automation was built for. Per this task's own instructions, no fix was forced where none was needed.

**One real observation, flagged but not acted on (very low priority, no live impact):** `msg.ReceivedTime` as returned by `win32com`/`pywintypes` carries a tzinfo label of `(UTC+00:00) Dublin, Edinburgh, Lisbon, London` even right now, in August, while this machine's actual local time is BST (UTC+1) -- e.g. the probe's `datetime.now().astimezone()` shows `+01:00` at the same moment `ReceivedTime` is labelled `+00:00`. The raw hour:minute:second digits on `ReceivedTime` (08:06:47) match Kevin's own stated local wall-clock observation exactly, which means the pywintypes value is very likely **already local time with an incorrectly-attached UTC tzinfo label**, not a true UTC value needing +1 conversion. This does not affect the date-comparison bug investigated here (date component is unaffected either way, and arrivals are always mid-morning, nowhere near a midnight boundary), so it was not fixed. Worth remembering if a future bug ever involves comparing `ReceivedTime` against a genuine UTC value, or an email arriving very close to midnight.

**Verified live end-to-end, for real, this session (not just re-reading logs):**
- Triggered `Start-ScheduledTask -TaskName 'HRIS Dashboard Morning Refresh'` for real at 12:00:08. Task ran to completion by 12:00:29 (`Get-ScheduledTask` polled directly until `State` left `Running`), `LastTaskResult: 0`.
- `osm_auto_refresh_last_run.log` (fresh from this real run, not a stale read) shows: folder now 53 items, today's email found (`ReceivedTime` 2026-08-22 08:06:47), attachment saved, `Update HRIS Dashboard.bat` → `import_osm_report.py` ran unmodified, 31 tickets parsed, `data/tickets.json` pushed (SHA `29b0b32e...`), `push_automation_status.py` pushed `data/last_automated_run.json` with `status: success`, `step: dashboard_update_ok`.
- Confirmed directly via `raw.githubusercontent.com` (bypasses GitHub Pages' build-cache lag, which was still showing stale 21 Aug data at the time of checking -- expected, not a bug, per this pipeline's own "allow ~60s" note) that `main` genuinely has `data/tickets.json` (`updated: 2026-08-22T12:00:19`) and `data/last_automated_run.json` (`status: success`, `timestamp: 2026-08-22T12:00:21`).
- Confirmed commits landed on `main`: `1b5c0ba2` ("OSM import — 2026-08-22 12:00") and `3fb07c34` ("automation status -- success (2026-08-22 12:00)").

---

## Session 2026-08-22 (afternoon) -- Kevin decided both open items from the morning: trigger moved later, retry mechanism built and live-tested (Drew, Kevin's explicit decision)

**Scope:** Kevin decided both items left open by the late-morning investigation above: (1) move the 08:30 trigger later, (2) add a retry mechanism for a morning where the OSM mail rule files the report late. Requirements: don't touch the manual `Update HRIS Dashboard.bat` path; keep "Last automated refresh" accurate (a real retry success shows its real time, a run of all-fail attempts shows failure clearly, not blurred); keep the append/early-diagnostics logging quality; verify live, not just in the abstract.

**Trigger schedule:** `HRIS Dashboard Morning Refresh`'s single 08:30 trigger replaced with three daily triggers on the SAME task (same Action/Principal/Settings, confirmed unchanged via `Get-ScheduledTask` before/after) — **08:45 primary, 09:15 and 09:45 retries**. Typical arrival is 08:01–08:16; 08:45 gives real buffer, and the two 30-minute-spaced retries cover a late-filing morning without waiting until the next day or leaning on the manual fallback for routine delays. `NextRunTime` confirmed live as `23/08/2026 08:45:00`.

**Retry-guard mechanism, new file `check_last_run_status.py`:** every trigger (primary and both retries) now runs this check FIRST, before touching Outlook or GitHub. It reads the live `data/last_automated_run.json`; if today's status is already `success`, it prints `SKIP` and `Run_HRIS_Auto_Refresh.bat` exits 0 immediately — no Outlook connection, no GitHub push, and critically, `data/last_automated_run.json`'s real first-success timestamp is never overwritten by a later redundant run. If today hasn't succeeded yet, it prints `RUN:<attempt>:<max_attempts>`, where `<attempt>` is a **wall-clock-time bucket** (before 09:00 → 1, before 09:30 → 2, else → 3) matching the three trigger times — deliberately stateless (no persisted per-day counter file that could drift out of sync from a stray manual run or missed trigger). A check-script download failure, or any other unexpected error, defaults to `RUN:1:3` rather than silently skipping a legitimate run. `push_automation_status.py` gained optional `--attempt`/`--max-attempts` fields (default 1/1), now passed on every one of `Run_HRIS_Auto_Refresh.bat`'s five status-push call sites. `index.html`'s badge now appends "— attempt N of 3" on a failure, and "no more automated retries today — run Update HRIS Dashboard.bat manually if needed" once N reaches 3, so three real failed attempts read as one clear final state, not a confusing blur.

**A real bug caught before it shipped:** the retry-guard's first draft used a multi-line `if errorlevel 1 (...) else (...)` block that set `CHECK_RESULT` and read it back within the SAME block — exactly this file's own previously-documented cmd.exe trap (parenthesized blocks expand `%VAR%` once, at parse time, so a variable set earlier in the same block reads back stale). Caught before any live test, rewritten using sequential `goto`-driven lines (each `%VAR%` expands fresh at the line that actually executes), which this file's own top-of-file lesson explicitly recommends over parens for exactly this reason. The one remaining parenthesized fragment (`for /f ... do if ... (set "A=%%B" & set "MAX=%%C")`) only ever SETS variables, never reads them back in the same block, so it needed no delayed-expansion fix.

**Verified live, for real, this session (not just reasoned about in the abstract):**
- `check_last_run_status.py` run directly against genuine live production data (today's real `success` record, timestamp `2026-08-22T12:00:21`) → printed `SKIP`, confirmed correct.
- 5 scenarios unit-tested against the actual imported module with mocked `requests` (attempt-bucket boundaries at 08:44/08:45/08:59/09:00/09:15/09:29/09:30/09:45/11:58; failed-today at 09:20 → `RUN:2:3`; stale success from yesterday → `RUN:3:3`; network exception → `RUN:3:3` fallback; genuine today-success → `SKIP`) — all passed.
- RUN branch proven against a **genuine live network fetch**, not just a mock: pushed a synthetic `status: failure` record for today to a throwaway branch (`test-retry-guard-22aug`, deleted after the test), pointed a scratch copy of the real script at that branch's raw URL, ran it for real → printed `RUN:3:3` (correct bucket for the current afternoon time). Throwaway branch deleted immediately after.
- The `.bat`'s new `goto`-based branching tested in isolation against all 5 realistic inputs (`SKIP`, `RUN:1:3`, `RUN:2:3`, `RUN:3:3`, a simulated check-script download failure) — each set `ATTEMPT_NUM`/`MAX_ATTEMPTS` correctly and branched correctly.
- The real, just-deployed `.bat` run directly (SKIP path, since today had already succeeded): logged the new skip message, exited 0, and `data/last_automated_run.json` on `main` confirmed byte-identical before/after (still the original `12:00:21` success record — no blur, no redundant push).
- The REAL registered Scheduled Task triggered end-to-end (`Start-ScheduledTask`), running through the genuine chain — Task Scheduler → `wscript.exe` (hidden) → `Run HRIS Auto-Refresh Hidden.vbs` → the freshly-deployed Desktop `.bat` — `LastTaskResult: 0`, fresh log confirms the same correct skip behaviour under real automated (not manually-invoked) execution.
- Deployed to `D:\OneDrive - lelitte.com\Desktop\Run HRIS Auto-Refresh.bat` with CRLF line endings (confirmed via `file`), matching this machine's established convention.
- All four changed/new files (`check_last_run_status.py`, `push_automation_status.py`, `Run_HRIS_Auto_Refresh.bat`, `index.html`) pushed to `main` via the GitHub Contents API per this repo's own `AGENT_MODEL.md` (not `git push`), each independently confirmed live via `raw.githubusercontent.com`. Commits: `d6dfd506` (new script), `0972d487` (status script), `6c8fd776` (orchestrator), `a94dc1d4` (dashboard).

**Untouched, as required:** `Update HRIS Dashboard.bat`, `import_osm_report.py`, and the manual pipeline path — zero changes. `SCRIPT_SHA` pin (that file's own supply-chain protection) not touched since `import_osm_report.py` itself wasn't changed.

**First real test of the new schedule under a genuine "hasn't succeeded yet" morning:** tomorrow, **23 Aug 2026, 08:45** (primary trigger). Everything tested live this session proves the mechanism works correctly; what hasn't and can't be tested without waiting for tomorrow is a full real morning cycle where the primary run fails, a retry actually re-runs the fetch+update pipeline from a cold start, and succeeds — the RUN branch's *decision logic* is proven live, but not yet a full retry executing Steps 1+2 against a live "not yet arrived" mailbox state (that's the routine day-to-day Step 1/2 mechanics, already proven live repeatedly in the 20-22 Aug sessions, not new code from this session).

~~**Decision still open from the 08:30 entry, unchanged by this session:** whether Kevin wants a later trigger time, a retry-later mechanism, or to just rely on the manual fallback for occasional late-report mornings.~~ **RESOLVED by the 2026-08-22 (afternoon) session above** -- Kevin decided both: later trigger (08:45) and a retry mechanism (09:15/09:45), both built and live-verified as of that session. Left here struck through rather than deleted so the decision trail stays visible.

---

## Session 2026-08-25 (morning) -- 9am "not updated" report investigated; confirmed the 22 Aug retry mechanism working exactly as designed, no code changed (Drew, report-only)

**Scope:** Kevin reported at 9am the dashboard looked stale. Investigated live rather than assuming a bug.

**Root cause: not a bug.** The 08:45 primary trigger fired on time and correctly failed loudly (today's OSM email hadn't landed in `Inbox/Reports/OSM` yet -- Kevin's 9am report was accurate at that moment). The 09:15 retry then fired, found the now-arrived email, and succeeded end-to-end -- 48 tickets parsed, `data/tickets.json` and `data/last_automated_run.json` (`status: success`, `attempt: 2 of 3`) both pushed and confirmed live. This is the first real morning the 22 Aug retry mechanism was tested against a genuine "primary fails, retry recovers" case, and it worked unattended with no manual intervention needed. No code or config changed. Full timeline with exact commit SHAs and timestamps in `RESUME.md`'s superseded 25 Aug morning entry.

---

## Session 2026-08-25 (afternoon) -- Friendly, non-technical status messaging replacing raw error/attempt internals on the dashboard UI (Drew, Kevin-approved via screenshots)

**Scope:** Same-day follow-up, separate request. Kevin wants a friendly message instead of raw/technical error text on the dashboard when data is stale, missing, or a fetch/retry has failed -- mirroring a similar ask he made separately for Linda's chat error on `hr-fa-knowledge-base` (Markey's repo, not touched here).

**Found:** the automated-refresh badge (`loadAutomationStatus()` in `index.html`) showed raw internals directly in the visible text on any failure state -- e.g. `Failed (fetch_failed) — attempt 1 of 3` in red, even on attempt 1, which is normal in-progress retry behaviour rather than an actual problem. The "no ticket data" error card printed a raw `Error: HTTP 404`-style line under its heading.

**Fixed, commit `ad3638a6`:** the badge now has three distinct visible states -- green **"up to date"** on success; a new neutral blue **"Data is refreshing — check back shortly."** (new `.main-header-auto.pending` CSS class, same established colour system) while a retry is still pending; red **"Automatic refresh didn't complete this morning — data may be a little behind. Run Update HRIS Dashboard.bat if you need it sooner."** only once all 3 retries are genuinely exhausted. Internal step/detail codes moved to the badge's hover `title` tooltip, not the visible text. The "no ticket data" error card now shows friendly copy only, with the raw fetch error also moved to a `title` tooltip. The existing "no automated run recorded yet" grey state and empty-ticket-list state were already friendly and left unchanged.

**Verified for real before pushing:** built a local Playwright test harness (`python -m http.server` + real Chromium via `playwright.sync_api`) serving the actual edited `index.html` against four synthetic `data/last_automated_run.json` variants (success / mid-retry / exhausted-retries / file-missing) plus a forced ticket-data-404 case -- five real rendered PNG screenshots, not descriptions. Per standing rule (screenshots required for visual-change approval), presented all five to Kevin before touching `main`; Kevin reviewed and approved. Pushed via the GitHub Contents API (this repo's own convention, not `git push`) and confirmed the new strings genuinely live on `main` via `raw.githubusercontent.com` (cache-busted) afterward, not assumed from the push response alone.

**Deliberately not touched:** Linda AI's own `"Connection error: Failed to fetch"` message in this same `index.html` -- same pattern as the `hr-fa-knowledge-base` Linda error Kevin asked Markey to fix separately. Flagged, not fixed, to avoid overlapping Markey's parallel work.

**Untouched, as required:** `data/last_automated_run.json`'s schema and the automation pipeline that writes it (`push_automation_status.py`, `check_last_run_status.py`, `Run_HRIS_Auto_Refresh.bat`) -- display-only change, no automation logic touched.

Full detail in `RESUME.md`'s latest entry (same session).

---

## Session 2026-08-27 (~10:30 BST) -- "Update HRIS Dashboard.bat not working" investigated; fresh export was already live, no push made; stale Desktop `.bat` + "better way" proposal recorded (Drew, via coordinator)

**Trigger:** Kevin reported the manual `.bat` "is not working" and asked for the
dashboard to be updated from `C:\Users\admin\Downloads\All Open Tasks by Team.xlsx`.

**Outcome: nothing needed pushing.** The fresh export was already live before
investigation began:
- Live `data/tickets.json` on `main` = commit `7b50878` "OSM import — 2026-08-27
  10:20" (Kevin's own manual `.bat` run at 10:19:58 today). `report_file: All Open
  Tasks by Team.xlsx`, `total 44 / assigned 40 / unassigned 4 / stale 6 / oldest 189`.
- Verified byte-identical: re-parsed the Downloads `.xlsx` (mtime 10:16) with the
  live script's own functions in a no-push dry-run — every ticket matches; only
  the `updated` timestamps differ (expected).
- Pages build `built` 09:20 UTC; `index.html` cache-busts its fetch; real
  Playwright screenshot of the live site confirms the 10:20 data is showing.

**No reproducible hard failure of the `.bat`.** Ran its exact mechanism (curl
`main` → `pip install openpyxl requests` → `python %TEMP%\import_osm_report.py`)
with push disabled: curl OK, pip OK, parsed 44 tickets cleanly.

**Real underlying issue — stale Desktop copy + fragile file pick:**
- `D:\OneDrive - lelitte.com\Desktop\Update HRIS Dashboard.bat` is the **2 Jul
  2026 version** (unpinned `main`, `pip install openpyxl requests` — no `xlrd`).
  Missing repo fixes `d3e4c24`, `a23f1dd`, `7b3014b`. Works today only because
  `xlrd 2.0.2` is already installed and `main`'s script still equals pinned SHA
  `e503509`.
- `import_osm_report.py` top-level `import xlrd` hard-exits if `xlrd` absent, even
  for `.xlsx` runs; `find_report()` silently takes the newest matching file, so a
  run started before the manual export finishes downloading (or while Excel locks
  it) imports the 08:01 morning `- auto.xls` or throws a lock error.
- Most probable trigger for Kevin's experience: first run before the `.xlsx` was
  ready; retry at 10:19 worked. (A pre-push `.bat` failure leaves no git trace.)
  Precedent: 25 Aug "not updated" was also not-a-bug.

**Morning auto path (Scheduled Task "HRIS Dashboard Morning Refresh"):** NOT
affected — `Run_HRIS_Auto_Refresh.bat` pulls `Update HRIS Dashboard.bat` fresh
from `main` each run, so it uses the good pinned repo copy. Today it succeeded on
attempt 1 (08:47, `last_automated_run.json` = success). Next run 28 Aug 08:45.

**Separate pre-existing breakage (unchanged, not this issue):** GitHub Actions
SAASIT scrape (`generate_dashboard.py`) still fails `ISM_4001 session expired`
(last diag run #159, 26 Aug 16:16 UTC). Needs Kevin's Oxford SSO+MFA re-login via
`Refresh Session.bat`. The dashboard "Refresh" button triggers this same failing
workflow.

**"Better way" proposal for the manual mid-day top-up — awaiting Kevin's pick.**
Full text in RESUME.md (latest section). Summary: Option 1 re-sync + harden the
script (do regardless, ~1–2 h); Option 2 drag-and-drop file target (~2 h);
**Option 3 (recommended) folder-watch on Downloads via a Startup-folder poll loop,
reusing the `ai-usage-dashboard` pattern, so Kevin just saves the export (~3–4 h)**;
Option 4 browser upload via Cloudflare Worker (~1–2 d). All keep GitHub-only
writes, leave the Scheduled Task untouched, and are git-revertable.

**Next action:** Kevin picks an option (or approves Option 1 alone). No build
until then. No live-site push outstanding.

---

## Session 2026-08-27 (~11:00 BST) -- Option 1 (harden import_osm_report.py + re-sync Desktop .bat) + Option 3 (Startup Downloads watcher) built, tested, merged, deployed; watcher LIVE (Drew, via coordinator, Kevin-approved)

Follows straight on from the ~10:30 session below. Kevin picked Option 1 + Option 3
from the "better way" proposal.

**Shipped to `main`:** `207f9c9` (hardened script + watcher files, branch) ->
`bd93285` (merge) -> `aa77b25` (`.bat` SCRIPT_SHA -> `bd93285`) -> `b769903`
(`watch_downloads.config.json` script_sha -> `bd93285`). Branch
`harden-import-and-downloads-watcher` merged then deleted.

**Option 1 -- `import_osm_report.py` hardening.** Parsing / `tickets.json` schema
UNCHANGED (re-verified byte-identical against the live Downloads file, 44 tickets).
Added: lazy `xlrd` (xlsx-only runs no longer need it); `read_rows_with_retry()`
for locked / still-downloading files (clear message + 3x4s retry, no traceback,
no push); explicit-file override `OSM_IMPORT_FILE` / `--file` / bare `argv[1]`;
>20-min stale-file gate (prompt if interactive, warn-and-proceed if not, so the
Scheduled Task / watcher never hang); advisory single-writer lock
(`%TEMP%\hris_osm_import.lock`, auto-stale 15 min, bounded 90 s wait -- cannot
deadlock); GitHub Contents-API push race-retry on 409/422; timestamped first
line; all printed strings ASCII-only (legacy console codepage safety) except the
commit-message em-dash. 6 unit-style checks + watcher-style invocation all pass.

**Option 3 -- `watch_downloads.py` + `watch_downloads.config.json` +
`Start HRIS Downloads Watcher.vbs`.** Stdlib-only Startup-folder poll (~60 s) of
`C:\Users\admin\Downloads`. Runs the PINNED `import_osm_report.py` on a
settle-detected (2 stable polls) new `All Open Tasks by Team*.xlsx|xls` whose
`name+size+sha256` is not already in the state file. Kevin's mid-day step becomes
"save the export to Downloads".

**Collision-safety with "HRIS Dashboard Morning Refresh" (verified):**
hard-excludes any `- auto` name (the task saves `All Open Tasks by Team - auto.xls`);
fully idle during weekday 08:40-10:00 (files appearing then are held, not
dropped, and imported once the window clears); plus the advisory lock and the
push race-retry. The morning task itself is NOT modified -- it still pulls
`Update HRIS Dashboard.bat` fresh from `main`, so it auto-adopts the new pin.

**How a watcher-triggered import reaches `main`:** AUTO / direct -- the same
single `data/tickets.json` Contents-API commit the manual `.bat` and the morning
task already make unattended. Not staged, not a PR. Safe/revertible: only fires
on a genuinely new settled file; dedupe never repeats bytes; hardened script;
`index.html` never touched; revert = `git revert <sha>` on that one JSON commit
(Pages ~60-90 s). Kill switch: `"enabled": false` in the config (exits within one
poll) or delete the Startup `.vbs` + `taskkill` the `pythonw`.

**Deployed on DESKTOP-MJDJM64 (~10:56-11:00):**
`%LOCALAPPDATA%\hris-downloads-watcher\` holds the watcher, config, Startup
`.vbs`, pinned script, seeded `state.json`, `watcher.log`, `watcher.pid`. Startup
folder has the launcher. Desktop `Update HRIS Dashboard.bat` replaced with the
`main` copy (old one -> `Update HRIS Dashboard.bat.bak-20260827`). Watcher
started and confirmed running (one `pythonw`, matches `watcher.pid`), idle,
`script_sha=bd93285`, live `tickets.json` unchanged.

**MAINTENANCE (new):** the import-script SHA pin now lives in TWO files --
`Update HRIS Dashboard.bat` (`SCRIPT_SHA=`) and `watch_downloads.config.json`
(`"script_sha":`). Any future `import_osm_report.py` change must bump BOTH in the
same change and re-copy both to their deployed locations (Desktop / %LOCALAPPDATA%).
The running watcher re-reads its config every poll and auto-refetches the pinned
script when the sha changes. See RESUME.md (27 Aug ~11:00) for the checklist.

**Not this session:** GitHub Actions SAASIT scrape still needs Kevin's SSO
re-login (pre-existing, `ISM_4001`).

**Next action:** none required. Optionally, Kevin saves his next real mid-day
export to Downloads and confirms the dashboard updates within ~3-4 min
(`watcher.log` shows the run).
